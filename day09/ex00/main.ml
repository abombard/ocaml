let () =
    Printf.printf "Watchtower.add 1 2 -> %d\n" (Watchtower.Watchtower.add 1 2);
    Printf.printf "Watchtower.add 5 10 -> %d\n" (Watchtower.Watchtower.add 5 10);
    Printf.printf "Watchtower.add 12 0 -> %d\n" (Watchtower.Watchtower.add 12 0);
    Printf.printf "Watchtower.add 11 15 -> %d\n" (Watchtower.Watchtower.add 11 15);
    Printf.printf "Watchtower.sub 1 2 -> %d\n" (Watchtower.Watchtower.sub 1 2);
    Printf.printf "Watchtower.sub 5 10 -> %d\n" (Watchtower.Watchtower.sub 5 10);
    Printf.printf "Watchtower.sub 12 0 -> %d\n" (Watchtower.Watchtower.sub 12 0);
    Printf.printf "Watchtower.sub 11 15 -> %d\n" (Watchtower.Watchtower.sub 11 15)

